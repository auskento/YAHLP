# Certbot renewal cron job
# Run daily at 3 AM to renew certificates if needed
0 3 * * * /usr/bin/certbot renew --quiet --agree-tos && /usr/sbin/apache2ctl graceful >> /var/log/certbot-renewal.log 2>&1
