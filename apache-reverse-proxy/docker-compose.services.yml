# ═══════════════════════════════════════════════════════════════
# OPTIONAL: Complete Stack with Backend Services
# ═══════════════════════════════════════════════════════════════
# This file shows how to run the reverse proxy with backend
# services all in one docker-compose.
#
# To use this instead of the basic docker-compose.yml:
#   mv docker-compose.yml docker-compose.basic.yml
#   mv docker-compose.services.yml docker-compose.yml
#   docker-compose up -d
#
# Or run both files together:
#   docker-compose -f docker-compose.yml \
#                  -f docker-compose.services.yml up -d

version: '3.8'

services:
  # ═══════════════════════════════════════════════════════════════
  # REVERSE PROXY (Required)
  # ═══════════════════════════════════════════════════════════════
  apache-reverse-proxy:
    build: .
    container_name: apache-reverse-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    
    environment:
      DOMAIN: example.com
      EMAIL: admin@example.com
      
      # Enable services you're running below
      ENABLE_SONARR: "true"
      ENABLE_RADARR: "true"
      ENABLE_JELLYFIN: "true"
      
      # Disable others
      ENABLE_WHISPARR: "false"
      ENABLE_LIDARR: "false"
      ENABLE_READARR: "false"
      ENABLE_PROWLARR: "false"
      ENABLE_OVERSEERR: "false"
      ENABLE_EMBY: "false"
      ENABLE_PLEX: "false"
      ENABLE_TAUTULLI: "false"
      ENABLE_TRANSMISSION: "false"
      ENABLE_QBITTORRENT: "false"
      ENABLE_SABNZBD: "false"
      ENABLE_DELUGE: "false"
      
      ENABLE_AUTH_OFFICE365: "false"
      SSL_PROTOCOLS: "all -SSLv2 -SSLv3 -TLSv1 -TLSv1.1"
      SSL_CIPHERS: "HIGH:!aNULL:!MD5"
    
    volumes:
      - letsencrypt-data:/etc/letsencrypt
      - apache-logs:/var/log/apache2
    
    networks:
      - proxy-network
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ═══════════════════════════════════════════════════════════════
  # BACKEND SERVICES (Optional - Uncomment to use)
  # ═══════════════════════════════════════════════════════════════

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Australia/Melbourne
    volumes:
      - ./appdata/sonarr/config:/config
      - ./media:/media
    ports:
      - "8989:8989"
    networks:
      - proxy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8989/"]
      interval: 30s
      timeout: 10s
      retries: 3

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Australia/Melbourne
    volumes:
      - ./appdata/radarr/config:/config
      - ./media:/media
    ports:
      - "7878:7878"
    networks:
      - proxy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7878/"]
      interval: 30s
      timeout: 10s
      retries: 3

  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Australia/Melbourne
    volumes:
      - ./appdata/jellyfin/config:/config
      - ./media:/media
    ports:
      - "8096:8096"
    networks:
      - proxy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8096/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Whisparr (Uncomment if needed)
  # whisparr:
  #   image: lscr.io/linuxserver/whisparr:latest
  #   container_name: whisparr
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/whisparr/config:/config
  #     - ./media:/media
  #   ports:
  #     - "6969:6969"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Lidarr (Uncomment if needed)
  # lidarr:
  #   image: lscr.io/linuxserver/lidarr:latest
  #   container_name: lidarr
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/lidarr/config:/config
  #     - ./media:/media
  #   ports:
  #     - "8686:8686"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Readarr (Uncomment if needed)
  # readarr:
  #   image: lscr.io/linuxserver/readarr:latest
  #   container_name: readarr
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/readarr/config:/config
  #     - ./media:/media
  #   ports:
  #     - "8787:8787"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Prowlarr (Uncomment if needed)
  # prowlarr:
  #   image: lscr.io/linuxserver/prowlarr:latest
  #   container_name: prowlarr
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/prowlarr/config:/config
  #   ports:
  #     - "9696:9696"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Overseerr (Uncomment if needed)
  # overseerr:
  #   image: sctx/overseerr:latest
  #   container_name: overseerr
  #   environment:
  #     LOG_LEVEL: info
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/overseerr/config:/app/config
  #   ports:
  #     - "5055:5055"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Plex (Uncomment if needed)
  # plex:
  #   image: lscr.io/linuxserver/plex:latest
  #   container_name: plex
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #     VERSION: latest
  #   volumes:
  #     - ./appdata/plex/config:/config
  #     - ./media:/media
  #   ports:
  #     - "32400:32400"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Emby (Uncomment if needed)
  # emby:
  #   image: lscr.io/linuxserver/emby:latest
  #   container_name: emby
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/emby/config:/config
  #     - ./media:/media
  #   ports:
  #     - "8096:8096"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Tautulli (Uncomment if needed)
  # tautulli:
  #   image: lscr.io/linuxserver/tautulli:latest
  #   container_name: tautulli
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/tautulli/config:/config
  #   ports:
  #     - "8181:8181"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Transmission (Uncomment if needed)
  # transmission:
  #   image: lscr.io/linuxserver/transmission:latest
  #   container_name: transmission
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/transmission/config:/config
  #     - ./downloads:/downloads
  #   ports:
  #     - "6969:6969"
  #     - "6969:6969/udp"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # qBittorrent (Uncomment if needed)
  # qbittorrent:
  #   image: lscr.io/linuxserver/qbittorrent:latest
  #   container_name: qbittorrent
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #     WEBUI_PORT: 8080
  #   volumes:
  #     - ./appdata/qbittorrent/config:/config
  #     - ./downloads:/downloads
  #   ports:
  #     - "8080:8080"
  #     - "6881:6881"
  #     - "6881:6881/udp"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # SABnzbd (Uncomment if needed)
  # sabnzbd:
  #   image: lscr.io/linuxserver/sabnzbd:latest
  #   container_name: sabnzbd
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/sabnzbd/config:/config
  #     - ./downloads:/downloads
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

  # Deluge (Uncomment if needed)
  # deluge:
  #   image: lscr.io/linuxserver/deluge:latest
  #   container_name: deluge
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: Australia/Melbourne
  #   volumes:
  #     - ./appdata/deluge/config:/config
  #     - ./downloads:/downloads
  #   ports:
  #     - "8112:8112"
  #     - "6881:6881"
  #     - "6881:6881/udp"
  #   networks:
  #     - proxy-network
  #   restart: unless-stopped

volumes:
  letsencrypt-data:
    driver: local
  apache-logs:
    driver: local

networks:
  proxy-network:
    driver: bridge
