# Basic Authentication Configuration
# Protects all paths except health checks and static assets

<LocationMatch "^/(?!health|icons)">
    AuthType Basic
    AuthName "Authentication Required"
    AuthUserFile /etc/apache2/.htpasswd
    Require valid-user
</LocationMatch>
