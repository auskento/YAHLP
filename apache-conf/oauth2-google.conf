# Google OpenID Connect Configuration for Apache
# This configuration enables Google authentication for all proxied services

# Session Configuration for OIDC
OIDCSessionType server-cache

# Client Configuration - Provided via environment variables
OIDCClientID @@GOOGLE_CLIENT_ID@@
OIDCClientSecret @@GOOGLE_CLIENT_SECRET@@
OIDCRedirectURI @@GOOGLE_REDIRECT_URI@@

# Google OpenID Connect Provider Configuration
# Uses Google's public OIDC endpoint
OIDCProviderMetadataURL https://accounts.google.com/.well-known/openid-configuration

# Scopes - what information to request from Google
OIDCScope "openid profile email"

# Session configuration
OIDCSessionInactivityTimeout 3600
OIDCSessionMaxDuration 86400

# Claim configuration
OIDCClaimPrefix OIDC_
OIDCPassClaimsAs environment

# Encryption
OIDCCryptoPassphrase "@@GOOGLE_CRYPTO_PASSPHRASE@@"

# Info logging - set to DEBUG for troubleshooting
# OIDCDebug Off

# Validate SSL certificates (recommended for production)
OIDCSSLValidateServer On

# Claim settings - customize which claims are passed as headers
OIDCClaimDelimiter ;
OIDCPassUserInfoAs json

# Session cookie settings - use wildcard domain for subdomains
OIDCCookieDomain @@COOKIE_DOMAIN@@
OIDCCookieSameSite Off
